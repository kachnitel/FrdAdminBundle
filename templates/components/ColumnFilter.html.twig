<div {{ attributes }}>
    {% set filterType = this.type %}
    {% if this.type == 'enum' and filterMetadata.enumClass is not defined %}
        {% set filterType = 'text' %}
    {% endif %}

    {% include '@KachnitelAdmin/components/ColumnFilter/_' ~ filterType ~ '.html.twig' with {
        column: column,
        value: this.value,
        filterMetadata: filterMetadata
    } only %}
</div>
