<input
    type="number"
    class="form-control form-control-sm"
    placeholder="{{ filterMetadata.placeholder|default('Filter ' ~ column|humanize ~ '...') }}"
    data-model="debounce(300)|value"
    value="{{ value }}"
>
